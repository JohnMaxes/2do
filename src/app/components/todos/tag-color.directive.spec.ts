import { TagColorDirective } from './tag-color.directive';

describe('TagColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TagColorDirective();
    expect(directive).toBeTruthy();
  });
});
